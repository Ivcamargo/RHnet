services:
  rhnet:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    volumes:
      - rhnet_uploads:/app/uploads

volumes:
  rhnet_uploads:
