# =========================================
# Variáveis de Ambiente - RHNet
# Hostinger VPS Production
# =========================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env no servidor Hostinger
# 2. Preencha TODOS os valores abaixo
# 3. NUNCA commite o arquivo .env no Git!
# 4. Permissões: chmod 600 .env (somente owner pode ler)
#
# =========================================

# ============= AMBIENTE =============
NODE_ENV=production

# ============= SERVIDOR =============
# Porta do Express (Nginx vai fazer proxy)
PORT=5000

# Domínio público
REPLIT_DOMAINS=www.rhnet.online

# ============= BANCO DE DADOS =============
# IMPORTANTE: Atualize com suas credenciais do PostgreSQL no Hostinger
# Formato: postgresql://usuario:senha@localhost:5432/nome_banco
DATABASE_URL=postgresql://rhnet_user:SUA_SENHA_SEGURA_AQUI@localhost:5432/rhnet_db

# Variáveis individuais do PostgreSQL (opcional, mas algumas partes do código podem usar)
PGHOST=localhost
PGPORT=5432
PGDATABASE=rhnet_db
PGUSER=rhnet_user
PGPASSWORD=SUA_SENHA_SEGURA_AQUI

# ============= EMAIL (SendGrid) =============
# Para envio de emails (notificações, leads, etc)
# Obtenha em: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.sua_api_key_aqui

# Email remetente (precisa ser verificado no SendGrid)
FROM_EMAIL=noreply@rhnet.online

# Email para receber leads comerciais
SALES_EMAIL=infosis@infosis.com.br

# ============= AUTENTICAÇÃO =============
# Segredo para sessões (gere um token aleatório seguro)
# Execute: openssl rand -hex 32
SESSION_SECRET=gere_um_token_aleatorio_de_64_caracteres_aqui_use_openssl_rand

# ============= GEOLOCALIZAÇÃO =============
# APIs de geolocalização (se usar serviço pago)
# Atualmente usa Nominatim (OpenStreetMap) que é gratuito
# GEO_API_KEY=sua_chave_se_usar_servico_pago

# ============= ARMAZENAMENTO DE ARQUIVOS =============
# Caminho para upload de fotos (reconhecimento facial)
UPLOAD_PATH=/var/www/rhnet/uploads

# Tamanho máximo de upload (em bytes) - 10MB
MAX_FILE_SIZE=10485760

# ============= SEGURANÇA =============
# Tempo de vida da sessão (em milissegundos) - 7 dias
SESSION_TTL=604800000

# Habilitar HTTPS strict (após configurar SSL)
FORCE_HTTPS=true

# CORS origins permitidas (separadas por vírgula)
CORS_ORIGINS=https://www.rhnet.online,https://rhnet.online

# ============= TIMEZONE =============
# Fuso horário do sistema
TZ=America/Sao_Paulo

# ============= LOGS =============
# Nível de log (error, warn, info, debug)
LOG_LEVEL=info

# ============= RECURSOS OPCIONAIS =============
# Habilitar modo terminal/kiosk
ENABLE_KIOSK_MODE=true

# Habilitar reconhecimento facial
ENABLE_FACIAL_RECOGNITION=true

# Habilitar geofencing
ENABLE_GEOFENCING=true

# ============= BACKUP =============
# Email para receber notificações de backup
BACKUP_EMAIL=admin@rhnet.online

# Diretório de backups
BACKUP_DIR=/var/backups/rhnet

# =========================================
# CHECKLIST DE CONFIGURAÇÃO
# =========================================
# 
# [ ] DATABASE_URL configurado com suas credenciais
# [ ] SENDGRID_API_KEY configurado (ou comentado se não usar)
# [ ] SESSION_SECRET gerado (openssl rand -hex 32)
# [ ] Domínio atualizado em REPLIT_DOMAINS
# [ ] Permissões do arquivo: chmod 600 .env
# [ ] Testou conexão com banco: psql $DATABASE_URL
# [ ] Verificou que .env está em .gitignore
#
# =========================================
